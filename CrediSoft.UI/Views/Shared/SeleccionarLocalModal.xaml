<Window x:Class="CrediSoft.UI.Views.Shared.SeleccionarLocalModal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Seleccionar local"
        Width="420" Height="420"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Background="#F0F2F5"
        FontFamily="Segoe UI" FontSize="13"
        ShowInTaskbar="False">

    <Window.Resources>
        <Style x:Key="BtnOrange" TargetType="Button">
            <Setter Property="Background"      Value="#E65100"/>
            <Setter Property="Foreground"      Value="White"/>
            <Setter Property="FontWeight"      Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding"         Value="16,8"/>
            <Setter Property="Cursor"          Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#BF360C"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#BDBDBD"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="BtnGray" TargetType="Button">
            <Setter Property="Background"      Value="#ECEFF1"/>
            <Setter Property="Foreground"      Value="#37474F"/>
            <Setter Property="FontWeight"      Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush"     Value="#B0BEC5"/>
            <Setter Property="Padding"         Value="16,8"/>
            <Setter Property="Cursor"          Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#CFD8DC"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <DockPanel>

        <!-- Header -->
        <Border DockPanel.Dock="Top" Background="#37474F" Padding="16,10">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="&#xE707;" FontFamily="Segoe MDL2 Assets" FontSize="20"
                           Foreground="White" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <StackPanel>
                    <TextBlock Text="SELECCIONAR LOCAL" FontSize="14"
                               FontWeight="Bold" Foreground="White"/>
                    <TextBlock Text="Doble clic o Enter para confirmar"
                               FontSize="11" Foreground="#B0BEC5"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Botones pie -->
        <Border DockPanel.Dock="Bottom" Background="White" BorderBrush="#E0E0E0"
                BorderThickness="0,1,0,0" Padding="12,10">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Cancelar" Style="{StaticResource BtnGray}"
                        Margin="0,0,8,0" Click="OnCancelar"/>
                <Button x:Name="BtnSeleccionar" Content="Seleccionar" Style="{StaticResource BtnOrange}"
                        IsEnabled="False" Click="OnSeleccionar"/>
            </StackPanel>
        </Border>

        <!-- Buscador -->
        <Border DockPanel.Dock="Top" Margin="12,10,12,0"
                Background="White" BorderBrush="#CFD8DC" BorderThickness="1" CornerRadius="4">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="🔍" FontSize="14"
                           VerticalAlignment="Center" Margin="10,0,6,0"
                           Foreground="#90A4AE"/>
                <TextBox x:Name="TxtBuscar"
                         Grid.Column="1"
                         BorderThickness="0" Background="Transparent"
                         Padding="4,8" FontSize="13"
                         VerticalContentAlignment="Center"
                         TextChanged="OnBuscarChanged"
                         KeyDown="OnKeyDown">
                    <TextBox.Style>
                        <Style TargetType="TextBox">
                            <Style.Resources>
                                <VisualBrush x:Key="Hint" TileMode="None" Opacity="0.5"
                                             Stretch="None" AlignmentX="Left">
                                    <VisualBrush.Visual>
                                        <TextBlock Text="Buscar por nombre o código..."
                                                   Foreground="#90A4AE" FontSize="13"
                                                   Margin="2,0,0,0"/>
                                    </VisualBrush.Visual>
                                </VisualBrush>
                            </Style.Resources>
                            <Style.Triggers>
                                <Trigger Property="Text" Value="">
                                    <Setter Property="Background" Value="{StaticResource Hint}"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </TextBox.Style>
                </TextBox>
            </Grid>
        </Border>

        <!-- Lista de locales -->
        <Border Background="White" Margin="12,6,12,0"
                BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="4">
            <ListBox x:Name="ListLocales"
                     BorderThickness="0"
                     SelectionChanged="OnSelectionChanged"
                     MouseDoubleClick="OnDobleClick"
                     KeyDown="OnKeyDown">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="4,6">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="36"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Border Grid.Column="0" Background="#ECEFF1" CornerRadius="3"
                                    Width="28" Height="28" HorizontalAlignment="Left">
                                <TextBlock Text="{Binding IdLocal}" FontWeight="Bold"
                                           Foreground="#455A64" FontSize="12"
                                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="{Binding NombreLocal}" FontWeight="SemiBold"
                                           Foreground="#212121" FontSize="13"/>
                                <TextBlock Text="{Binding CiudadLocal}" FontSize="11"
                                           Foreground="#78909C"/>
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Padding"  Value="4,2"/>
                        <Setter Property="Cursor"   Value="Hand"/>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#E3F2FD"/>
                                <Setter Property="Foreground" Value="#0D47A1"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#F5F5F5"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </ListBox.ItemContainerStyle>
            </ListBox>
        </Border>

    </DockPanel>
</Window>
